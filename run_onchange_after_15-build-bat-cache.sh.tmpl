#!/bin/sh

set -eu

# bat theme hash: {{ include "dot_config/bat/themes/rose-pine.tmTheme" | sha256sum }}

if ! command -v bat >/dev/null 2>&1; then
	printf '%s\n' 'bat is required before its theme cache can be built.' >&2
	exit 1
fi

bat cache --build
